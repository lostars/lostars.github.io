const modal=document.getElementById("imageModal"),modalImage=document.getElementById("modalImage"),closeBtn=document.getElementById("closeBtn"),thumbnails=document.querySelectorAll(".gallery-item");let currentIndex=0;function openModal(e){currentIndex=e,modalImage.src=thumbnails[currentIndex].getAttribute("data-full"),modal.style.display="flex"}function closeModal(){modal.style.display="none"}function showPrev(){currentIndex=(currentIndex-1+thumbnails.length)%thumbnails.length,modalImage.src=thumbnails[currentIndex].getAttribute("data-full")}function showNext(){currentIndex=(currentIndex+1)%thumbnails.length,modalImage.src=thumbnails[currentIndex].getAttribute("data-full")}thumbnails.forEach(((e,t)=>{e.addEventListener("click",(()=>openModal(t)))})),closeBtn.addEventListener("click",closeModal),window.addEventListener("click",(e=>{e.target===modal&&closeModal()})),window.addEventListener("keydown",(e=>{"flex"===modal.style.display&&("ArrowLeft"===e.key?showPrev():"ArrowRight"===e.key&&showNext())}));